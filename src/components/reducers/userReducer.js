const { types } = require("../types/types");


const initialState = {
    name: '',
    search: []
}

export const userReducer = ( state = initialState, action) => {

    switch (action.type ) {
        default:
            return state
    }

}

